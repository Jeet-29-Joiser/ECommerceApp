<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.ecommerceapp.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_homeFragment_to_categoryFragment"
            app:destination="@id/categoryFragment" />
    </fragment>

    <fragment
        android:id="@+id/categoryFragment"
        android:name="com.example.ecommerceapp.ui.category.CategoryFragment"
        android:label="Category"
        tools:layout="@layout/fragment_category">

        <argument
            android:name="categorySlug"
            app:argType="string" />

        <argument
            android:name="categoryTitle"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/allCategoriesFragment"
        android:name="com.example.ecommerceapp.ui.category.AllCategoriesFragment"
        android:label="All Categories"
        tools:layout="@layout/fragment_all_categories" />
    <fragment
        android:id="@+id/productDetailFragment"
        android:name="com.example.ecommerceapp.ui.detail.ProductDetailFragment"
        android:label="Product Detail"
        tools:layout="@layout/fragment_product_detail" />
</navigation>